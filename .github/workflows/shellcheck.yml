name: Check shell scripts
on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    paths:
      - 'e2e/**'
      - '.github/**'
      - 'scripts/release.sh'
  push:
    branches:
      - master

jobs:
  check_macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install shellcheck
        run: brew install shellcheck && shellcheck -V
      - name: Check e2e scripts
        run: shellcheck e2e/tests-dfx/*.bash e2e/tests-replica/*.bash
      - name: Check release script
        run: shellcheck scripts/release.sh
  check_ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install shellcheck
        run: sudo apt-get install -f shellcheck && shellcheck -V
      - name: Check e2e scripts
        run: shellcheck e2e/tests-dfx/*.bash e2e/tests-replica/*.bash
      - name: Check release script
        run: shellcheck scripts/release.sh
